# OpenDocViewer / src/index.jsx

Files: 1. Lines: 59. JSDoc symbols: 2.

## src/index.jsx

Determine environment and set a sensible client-side log level.

Local imports: ./styles.css, src/i18n.js, src/logging/systemLogger.js, src/app/AppBootstrap.jsx, src/contexts/ThemeProvider.jsx

Symbols:

- `isDev` (constant) - Determine environment and set a sensible client-side log level.
- `container` (constant) - Mount the app into #root.
